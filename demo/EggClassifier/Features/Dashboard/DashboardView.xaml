<UserControl x:Class="EggClassifier.Features.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="680" d:DesignWidth="1060">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="&#x1F4CA; 대시보드"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}"
                   Margin="0,0,0,20"/>

        <!-- 통계 카드 3개 -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 총 검사 수 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="총 검사 수"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalInspections}"
                               FontSize="36" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- 정상 -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="5,0,5,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="정상"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding NormalCount}"
                               FontSize="36" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- 불량 -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="불량"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding DefectCount}"
                               FontSize="36" FontWeight="Bold"
                               Foreground="{StaticResource DangerBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 최근 로그 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>


                    <DockPanel Grid.Column="0" LastChildFill="False">
                        <TextBlock Text="최근 검사 로그" 
                           FontSize="16" FontWeight="Bold" 
                           Foreground="{StaticResource TextBrush}" 
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"
                           Margin="0,0,20,0"/>
                        <StackPanel Orientation="Horizontal"
                                    DockPanel.Dock="Right"
                                    VerticalAlignment="Center">
                            <RadioButton Content="전체" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="-1" IsChecked="True" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Content="정상" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="0" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Content="크랙" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="1" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Content="이물질" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="2" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Content="탈색" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="3" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Content="외형이상" GroupName="EggFilter" Command="{Binding SetFilterCommand}" CommandParameter="4" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <ListBox Grid.Column="0" 
                     ItemsSource="{Binding InspectionLogs}"
                     SelectedItem="{Binding SelectedLog}"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#1AFFFFFF" BorderThickness="0,0,0,1" Padding="8">
                                    <TextBlock Text="{Binding DisplayText}" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="Consolas"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border Grid.Column="1" Margin="15,0,0,0" Background="#1AFFFFFF" CornerRadius="8">
                        <Image Source="{Binding SelectedImageSource}" Stretch="Uniform" Margin="10"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
